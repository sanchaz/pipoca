# cálculo de Pi por desenvolvimento em série
var real pi[1];
real start()
  var i, n

  'Cálculo de Pi por desenvolvimento em série\n\
 Indique o número de iterações: '!

  if i? > 0 then
      n := 1
      while n < i do
	  pi[0] := pi[0] + -1.0**(n-1)/(2*n-1);
          n++
      done
      'Pi = ' + pi[0] * 4!!
  else
    'O número de iterações deve ser um inteiro positivo\n'!
  endif
end
